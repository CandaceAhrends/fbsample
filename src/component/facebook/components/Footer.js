import React from "react";

const Footer = () => {
  return (
    <ul
      style={{
        height: "100px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <li>You're Caught Up For Now</li>
    </ul>
  );
};

export default Footer;
